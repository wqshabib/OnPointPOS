--****For POSSUMMasterDB***

insert into OutletUser(Id,userId,OutletId)
select 'ValueOfNewGuid','ValueOfUserTableUserID','ValueOfOutletTableOutletID'

select newid()

select * from Users

select * from Outlet
